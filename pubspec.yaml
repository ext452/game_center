name: game_services
description: A new flutter plugin project.
version: 0.0.1
author:
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
flutter:
  plugin:
    androidPackage: com.example.gameservices
    pluginClass: GameServicesPlugin

